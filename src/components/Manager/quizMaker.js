// yash's quiz maker from test goes here.
import { uniqueID } from './popupDialog';

const QuizMaker = () => {
    return (
        <div>
            <div>{uniqueID}</div>
        </div>
    );
};

export default QuizMaker;
